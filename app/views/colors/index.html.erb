<p id="notice"><%= notice %></p>

<h1>Colors</h1>


<table class="table-bordered" width="100%" cellspacing="0">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Código Hexadecimal</th>
      <th>Opciones</th>
    </tr>
  </thead>

  <tbody>
    <% @colors.each do |color| %>
      <tr>
        <td><%= color.name %></td>
        <td><%= color.hex_code %></td>
        <td>
          <%= link_to 'Mostrar', color %>
          <%= link_to 'Editar', edit_color_path(color) %>
          <%= link_to 'Eliminar', color, method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Color', new_color_path %>
